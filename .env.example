# LLM — pick one provider (OpenAI takes priority if both are set)

# Option A: OpenAI
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# Option B: Ollama (local)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen3-8b

# Search
TAVILY_API_KEY=tvly-...
